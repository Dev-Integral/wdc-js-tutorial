
    let sum = 4 + 5 // Expected result 9
    let difference = 18 - 12 // Expected Result 6
    let product = 3 * 4 // Expected result 12
    let quotient = 20 / 5 //Expected result 4
    let remainder = 22 % 5 // Expected result 2

    // Incrementing/Decrementing a number
    let myVariable = 25;
 
    myVariable = myVariable + 1; // Expected result _;
    myVariable++ // Short cut;
    myVariable+=1 // Shortcut;

    myVariable = myVariable - 1; // Expected result _;
    myVariable-- // Short cut;
    myVariable-=1 // Shortcut; Called augumented maths
    
    myVariable = myVariable * 2 // Expected Result
    myVariable *=2  //shortcut
    myVariable = myVariable - 1 //

    // Decimals (Floating) numbers
    let myDecimals = 20.50; // Perform operations the same way we did for integers

    //BODMAS
    let x = 5 * 4 - 10 / 10;
    let y = 5 * (4 - 10 ) / 10;
    console.log(x, y);